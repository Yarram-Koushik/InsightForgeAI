[project]
name = "insightforgeai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "koushik", email = "yerramkoushik4@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.5",
    "openpyxl>=3.1.5",
    "pandas>=3.0.5",
    "pyarrow>=25.0.0",
    "python-dotenv>=1.2.2",
    "streamlit>=1.59.1",
]

[project.scripts]
insightforgeai = "insightforgeai:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
